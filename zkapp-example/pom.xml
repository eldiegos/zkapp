<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.example.zkapp</groupId>
        <artifactId>zkapp</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>zkapp-example</artifactId>
    <packaging>war</packaging>

    <name>zkapp-example</name>
    <description>
        Aplicacion de ejemplo minima que consume unicamente zkapp-spring-boot-starter y demuestra
        login, navegacion tras autenticacion, CurrentUser, sesion y logout, empaquetada como WAR
        para Tomcat 11.
    </description>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>com.example.zkapp</groupId>
                <artifactId>zkapp-bom</artifactId>
                <version>${project.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.example.zkapp</groupId>
            <artifactId>zkapp-spring-boot-starter</artifactId>
        </dependency>

        <!-- Embebido para "mvn spring-boot:run" / WAR ejecutable en desarrollo. En un despliegue
             real sobre Tomcat 11 externo, el contenedor aporta el servlet-api y esta dependencia
             queda sin efecto en tiempo de ejecucion (scope provided). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-tomcat</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Spring Boot 4 extrajo el slice de test @AutoConfigureMockMvc/MockMvc a este modulo
             dedicado (antes vivia en spring-boot-test-autoconfigure). Solo se usa en tests: la
             aplicacion no usa Spring MVC en tiempo de ejecucion (ZK gestiona sus propios servlets). -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>zkapp-example</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludeDevtools>true</excludeDevtools>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
